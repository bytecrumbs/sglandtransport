stages:
  - test

variables:
  LC_ALL: "en_US.UTF-8"
  LANG: "en_US.UTF-8"

before_script:
  - pwd
  - cd ios

unit_test:
  stage: test
  tags:
    - unit_test
  script:
    - fastlane test
  artifacts:
    paths:
      - coverage
  only:
    - development
    - feature/*
