# M7 — Polish & parity pass

**Status:** ⬜ Pending
**Depends on:** M6

## Goal

Close the visual/UX gap to the Flutter app and lock in parity as automated snapshot baselines.

## Scope

- [ ] **Hero animation** rebuilt natively in SwiftUI for the home header (replaces Flare `city.flr`/
      `bus.flr`; ~32% height scrolling header). Ref: `dashboard_screen.dart`.
- [ ] **Convex bottom bar** styling to match `convex_bottom_bar` (elevated/notched look), replacing
      the M1 placeholder bar. Ref: `main_bottom_app_bar.dart`.
- [ ] **Staggered list-in animation** (~250ms slide-up 30pt + fade) on nearby/favorites/search/
      arrivals lists. Ref: `staggered_animation.dart`.
- [ ] Drawer refined toward a side drawer (from the M1 sheet).
- [ ] Empty/error states, **dark-mode** check, Dynamic Type / accessibility pass.
- [ ] Screen-by-screen UX comparison vs. Flutter; **promote signed-off parity screenshots to snapshot
      baselines** (e.g. swift-snapshot-testing) so future changes are caught automatically.

## Verification

- Side-by-side review across the full screen inventory shows no UX regressions.
- Snapshot baseline suite is green and wired into CI (M8).
