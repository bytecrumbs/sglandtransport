# M8 — Release plumbing (Fastlane + CI + TestFlight)

**Status:** ⬜ Pending
**Depends on:** M7

## Goal

Ship the native app as an **update to the existing App Store listing**, with CI building/testing it
and the API key injected from secrets — preserving existing users' data on upgrade.

## Scope

- [ ] Fastlane lane for the native target (reuse existing **match** signing: team `78XZ3S3HJ5`, ASC
      `122316894`, certs repo). Same bundle id `com.saschaderungs.ltaDatamall` → provisioning carries
      over. Ref: current `ios/fastlane/*`, `.github/workflows/beta.yml`.
- [ ] CI workflow: `xcodegen generate` → build → run **unit + UI tests** on an iOS 17 simulator →
      snapshot baselines (from M7). Inject `LTA_DATAMALL_API_KEY` from the existing GitHub secret into
      `Secrets.local.xcconfig` (or an equivalent build setting). Replaces the Flutter `flutter test`
      gate for iOS.
- [ ] Versioning: keep the `21.0.0+timestamp` scheme; bump build number per release.
- [ ] dSYM upload to Crashlytics in the release lane (from M1b).
- [ ] TestFlight upload lane.

## Verification

- TestFlight build installs **as an update over the existing app** on a device that has the current
  Flutter app, and **favorites + last-selected tab are preserved** (validates M4 end-to-end).
- CI is green: build + unit + UI + snapshot tests on the simulator.
- Crash in the TestFlight build symbolicates in Crashlytics.
