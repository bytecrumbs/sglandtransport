# M6 — Favorites + Service details + Route timeline + Search + About/Share/Review

**Status:** ⬜ Pending
**Depends on:** M5

> **Parity gate applies:** each feature done only when its XCUITest passes **and** its parity
> screenshot is signed off vs. the Flutter app.

## Goal

The remaining feature screens, bringing the app to full feature parity.

## Scope

### Favorite Buses (Tab 1)
- [ ] `getFavoriteBusServices` (port `bus_arrivals_service.dart`): grouped by stop (header + arrival
      cards), distance-sorted, auto-refresh on the arrival interval; empty-state copy preserved.
      Ref: `favorites/*`.

### Bus Service details (full-screen)
- [ ] Info table: operator, category, AM/PM peak/offpeak frequencies. Ref: `bus_service_details.dart`.
- [ ] **Route timeline** (port `bus_route*.dart` + `bus_routes_service.dart`): vertical connector +
      dots; **previous stops** italic/disabled with the synthetic "{N} previous stops" row when the
      boarding stop isn't sequence 1; future stops tappable → Arrivals.

### Search
- [ ] Search over cached stops (description/code/roadName) with **substring highlighting**; renders
      stop cards; logs `logSearch` to Analytics on clear/close. Ref: `search/*`,
      `custom_search_delegate.dart`, `search_result.dart`.

### About / Share / Review
- [ ] About: rich-text links (LTA Data Mall, website, Twitter, Facebook, GitHub, privacy) +
      "free | ad-free | open-source" + version. Ref: `about/*`.
- [ ] Share sheet ("Check out SG Land Transport here: https://sglandtransport.app") — already stubbed
      in M1; finalize.
- [ ] **Review prompt** (StoreKit `requestReview`): eligible after **>7 days AND ≥10 launches**, then
      request; track `firstLaunchKey`/`launchCountKey`. Ref: `rate_app_service.dart`.

## Verification

- Feature checklist exercised end-to-end; substring highlight, timeline states (previous/disabled/
  tappable), share sheet, review gating all behave as Flutter.
- XCUITests for each feature (stub mode) + parity screenshots signed off.
