# Screen capture tests
tests:
  - --target=test_driver/app.dart --driver=test_driver/app_test.dart --dart-define=IS_FLUTTER_DRIVE_RUN=TRUE

# Interim location of screenshots from tests
staging: /tmp/screenshots

recording: /tmp/screenshots_record

# A list of locales supported by the app
locales:
  - en-US

# A list of devices to emulate
devices:
  ios:
    iPhone Xs Max:
    iPhone X:
    iPhone 7 Plus:
    iPad Pro (12.9-inch) (2nd generation):
  # android:
  #   Nexus 5X:
  #   Nexus 6P:
  #   Nexus 9:
# Frame screenshots
frame: true
