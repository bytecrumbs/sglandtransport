import 'package:flutter/material.dart';

/// The main view that shows favorite Bus Stops
class BusFavouritesView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Bus Favourites'),
    );
  }
}
