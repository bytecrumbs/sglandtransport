import 'package:flutter/material.dart';

class BusArrivals extends StatelessWidget {
  static const String id = 'bus_arrivals_screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Bus Arrivals'),
      ),
    );
  }
}
